---
apiVersion: argoproj.io/v1alpha1
kind: EventBus
metadata:
  name: default
spec:
  nats:
    native:
      replicas: 3
      # Optional, authen strategy, "none" or "token", defaults to "none"
      auth: token
