---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: github-events
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: test-34.135.60.18.sslip.io
      http:
        paths:
          - path: /push
            pathType: Prefix
            backend:
              service:
                name: github-eventsource-svc
                port:
                  number: 12000
